# Skeptical review loop — 59 findings (5 rounds)

Severity: 11 blocker, 40 major, 8 minor. Answerable: 30 now, 14 framing, 11 needs-eval, 4 hard.


## BLOCKER

- **[needs-evaluation]** Because the polymorphic solver is unlowerable in DaCe without devirtualization, there is no 'polymorphic-but-lowered' DaCe baseline, so any measured C2 speedup is 'devirtualized binary vs. nothing-runs' and is unattributable to devirtualization per se -- it is conflated with the ability to compile at all.
  - *Why:* RQ4 promises to 'isolate the contributions beyond the scalar cascade' and report 'devirt+narrowing speedup ABOVE the LTO-injected-constant baseline,' but the only honest backend for that comparison (LLVM, where a polymorphic baseline exists) is exactly the one where the paper admits the benefit is unmeasured. A PC will see that the centerpiece speedup is either (a) measured against a non-runnable baseline in DaCe, or (b) not measured at all in the backend where a fair baseline exists -- leaving the C2 performance claim without a clean attribution on either platform.
  - *Fix:* Build the missing fair baseline: in an LLVM/Flang path, compare the indirect-dispatch binary (dispatch lowered but not devirtualized) against the devirtualized binary on the same hardware, and report that delta as the true C2 speedup. If DaCe cannot produce a dispatched baseline, state explicitly that the DaCe C2 number measures lowerability, not optimization, and carry the *performance* claim entirely on the LLVM comparison.

- **[answerable-now]** C2's related-work defense asserts 'we invert the direction' because CHA/WPD key off present type metadata and 'Julia infers types from runtime argument flow,' but speculative type-feedback devirtualization in production JITs (HotSpot CHA+type-profile, V8/SpiderMonkey hidden-class inline caches) already goes observed-VALUE -> concrete-type -> direct-call, and Agesen's Cartesian Product Algorithm builds the call-site type-set from value flow -- and the authors' OWN brief (G.0b R2) names Agesen CPA as 'the type-set-component analog,' yet CPA is neither cited nor distinguished anywhere in related.tex.
  - *Why:* The paper's single sharpest novelty sentence for the centerpiece ('we invert the direction... no prior analysis drives devirtualization from a recovered value domain') is contradicted by the closest analog the authors themselves identified in their research notes; a PC who checks the bib will find the load-bearing comparator (CPA) silently dropped, which reads as either an oversight or selective citation and damages credibility on the contribution the whole paper leads with.
  - *Fix:* Add Agesen CPA (1995) and speculative type-feedback devirtualization (HotSpot/V8 inline caches) to related.tex with explicit differentiators, and restate C2's delta as 'static, exact, config-file-sourced type-set ahead of deployment, no speculation/guard' rather than 'first to go value->type,' which the type-feedback line already does.

- **[answerable-now]** Read correctly, row C does NOT fold nz into the loops at all: 10 config globals are each reduced to an i1 'has init run?' flag and the loop bound/array stride is rematerialized at runtime as `select i1 %flag, i32 33, i32 0`, with zero `load i32, ptr @nz` value-loads remaining only because the value-load was replaced by a flag-load -- the optimizer could not prove the init store dominates all uses across the flattened call graph (setcoeff reads nz0 on a path the optimizer cannot order against domain()'s store), which is exactly the init-vs-compute phase-separation obstruction the paper calls fundamental.
  - *Why:* The paper simultaneously needs row C to (a) overcome the scalar barrier (to justify 'the standard cascade does the rest, our gain is only C2/C3') and (b) be defeated by phase separation (the structural-tax thesis); the artifact shows (b) wins even in the hand-injected closed single-program best case, so the abstract's claim that the LTO-injected-constant baseline 'recovers the scalar cases' is contradicted by the authors' own row-C IR -- the scalar gap is NOT closed by that baseline.
  - *Fix:* Either construct an injection channel where the constant store provably dominates (e.g. inject at module-data initialization / DATA / parameter, not inside dolu()), measure that it folds to literal bounds, and use THAT as the scalar baseline; or concede in the text that even closed-program LTO leaves a runtime presence-select and re-derive what fraction of the scalar gain the baseline actually captures.

- **[answerable-now]** The LU experiment that anchors the central 'configuration tax is structural' claim contains NO namelist READ at all: nz0 is simply an uninitialized module variable never assigned in the as-is TU, so row A measures 'compiler cannot fold an unset global' (trivial value-absence), not 'compiler cannot fold a value that arrived through an I/O barrier' as the README (line 34) and intro (lines 55-56) assert; grep confirms zero READ/NAMELIST statements and zero namelist-runtime symbols in lu.F90 and lu_O3.ll.
  - *Why:* The I/O barrier is the paper's named structural obstruction and its claimed delta over LTO/IPA, yet the flagship experiment never instantiates an I/O barrier -- the entire 'value-behind-I/O' half of the two-barrier story (Table 1 row A vs B) is asserted by analogy, not demonstrated, so a hostile PC can say the experiment proves nothing about the obstruction the paper is built on.
  - *Fix:* Add a variant where nz0 is actually populated by READ(NML=...) and show the namelist-runtime call (the real opaque barrier) blocks folding; alternatively retitle row A honestly as 'uninitialized external global' and stop claiming it models the namelist read. Desk-level fixture edit.

- **[answerable-now]** The LU table's row-C verdict '0 nz loads (yes -- folded)' is a measurement artifact: GlobalOpt demoted the global @_QMluEnz from i32 to a one-bit presence flag (`@_QMluEnz = internal unnamed_addr global i1 false`), and 6 `load i1, ptr @_QMluEnz` instructions plus 17 flag-gated branches survive in lto_O3.ll -- but reproduce.sh greps only `load i32, ptr @_QMluEnz`, which now matches nothing precisely because the type changed, not because the value folded.
  - *Why:* Row C is the paper's RQ1 centerpiece and the 'strongest standard-tool baseline' the abstract/intro/eval claim 'captures the scalar cases'; the single load-count number that the whole structural-tax narrative and the C1 'we supply the value, the row-C cascade fires' story rest on is a false positive produced by a hardcoded type in a grep, so the table's only quantitative claim is unsound as measured.
  - *Fix:* Change the metric to count loads of any width (`load i[0-9]+, ptr @nz`) and report the surviving 6 i1 loads; then either find a fixture where the value genuinely folds to a literal 33 in the loop bounds, or rewrite the row-C claim to state what actually happened (presence-flag demotion + runtime select), and stop calling it 'folded'.

- **[needs-evaluation]** The abstract and background assert as established fact that devirtualization and index-narrowing 'carry the remaining speedup' and are 'where the bulk of our speedup originates,' but this is precisely the RQ4 decomposition the evaluation section says does not yet exist ('THIS IS THE HEADLINE NUMBER AND IT DOES NOT EXIST YET'); the paper states its central empirical conclusion before measuring it.
  - *Why:* why a CGO PC would care
  - *Fix:* Run RQ4 (devirt+narrowing speedup measured ABOVE the LTO-injected-constant baseline) on GH200/MI300A and report the actual fraction of total speedup attributable to each domain kind; until then, soften the abstract from 'carry the remaining speedup' to a hypothesis, not a result.

- **[answerable-now]** The centerpiece C2 "reachability" claim — that devirtualization "enables lowering at all" and "no production Fortran toolchain performs this optimization" — is contradicted by Flang's own FIR design, which explicitly converts fir.dispatch to fir.call when the type is known at compile time via fir.dispatch_table/fir.dt_entry, and the devirt_wpd experiment never tested this path (it only tested the C++-only -fwhole-program-vtables flag and observed nvfortran's indirect call).
  - *Why:* C2 is declared the headline contribution; if a reviewer shows Flang already documents fir.dispatch->fir.call devirtualization from a compile-time-known type, the strong 'enables lowering at all' / 'no toolchain does this' framing reads as an overclaim about a DaCe-backend artifact rather than a fundamental Fortran gap, deflating the paper's central novelty.
  - *Fix:* Run flang-new on poly.f90 with a constant-folded allocate and with a SELECT TYPE type-is guard and report whether its FIR devirtualization pass fires (the docs say it should); then reframe C2 honestly as 'flang devirtualizes only when the concrete type is already syntactically present in a type-is guard or constant allocate — it cannot recover the type from a config-fixed integer tag,' making the real gap value->type recovery, not devirtualization-being-absent. Drop the 'unlowerable at all' reachability claim or scope it strictly to the DaCe frontend's current lowering, not Fortran toolchains generally.

- **[needs-evaluation]** The folding metric (count of residual 'load @nz' instructions) is untied to any performance quantity, and in this benchmark folding nz cannot affect storage at all: every array (u, rsd, a, b, ...) is dimensioned by the PARAMETER isiz1/isiz2/isiz3 (=33), not by nz/nz0, so recovering nz only changes loop trip counts, not allocation, layout, or footprint; the 8 residual loads at -O3 are an IR-grep proxy whose runtime relevance is never shown, and the PC already declared static codegen proxies insufficient.
  - *Why:* A PC that has stated static proxies are not enough will see RQ1/RQ2 still resting on a load-count grep with no wall-clock anchor, on a kernel where the folded value provably cannot touch the memory footprint that justifies the bandwidth-bound index-narrowing motivation, leaving the 'configuration tax paid every timestep' quantitatively unsubstantiated.
  - *Fix:* Convert the LU/velocity RQ1 evidence to a measured per-timestep wall-clock delta between the runtime-valued-bound binary and the specialized one on GH200/MI300A, reporting the vectorization/trip-count effects that actually move time; and demonstrate index narrowing on a kernel where the recovered bound genuinely sizes the storage (unlike LU, where isiz parameters do). Requires the cluster runs.

- **[needs-evaluation]** The paper's only stated success criterion -- 'recover >=X% of the manual gain' (evaluation.tex line 60) -- is benchmarked against a manual baseline (ecRad's 3x code-optimization factor) the authors neither reproduce nor isolate: by their own G.0b fidelity note the 3x bundles single-precision porting + loop restructuring + g-point hardcoding with no separable config-propagation component, and the 'hand-specialized binary' upper bound (H.4) is never constructed in either experiment.
  - *Why:* The flagship 'we automate what costs hundreds of person-hours' claim has no measured target to recover a percentage OF: the manual gain is a confounded literature number the authors cannot decompose, and no in-house hand-specialized binary exists as the upper bound, so the central success metric is undefined and its denominator unattributable -- a PC cannot verify the headline against anything concrete.
  - *Fix:* Construct an actual hand-specialized binary in-house (the literal NG_LW/NG_SW-hardcoded ecRad build) as a measured upper bound and report config-prop's gain as a fraction of THAT reproduced number, not the unreproduced confounded 3x; fix X to a committed threshold. Requires cluster runs plus building the manual baseline.

- **[answerable-now]** The recovery analysis maps each READ(NML=...) target to a module field and 'recovers the value from the deployment config file(s)', but Fortran namelist semantics are partial-by-design: a variable omitted from the deployed deck is NOT read and retains its source-level default (or its prior value), so the value that governs the compute phase lives in the PROGRAM, not the file, and reading only the deck yields the wrong constant for every defaulted variable.
  - *Why:* This is a silent-miscompile generator at the heart of C1's soundness: production ICON/QE decks specify only a small subset of the hundreds of namelist variables and rely on compiled-in defaults for the rest, so a recovery that folds 'the namelist value' will inject a garbage/empty value (or fall back overly often) for exactly the most common case, and the paper's safety story (fall back to generic, never miscompute) breaks if the analysis cannot even tell that a variable was defaulted rather than set.
  - *Fix:* State explicitly that recovery must fuse three value sources in namelist precedence order -- the compiled-in source default (the right-hand side of the module-field declaration), any DATA/init in the init phase, and the deck override -- and only certify a variable glacial when all three resolve to one value; add a sentence in recovery.tex and a defaulted-variable case to the lu_globals fixture; no cluster run needed.

- **[needs-evaluation]** There is no statistical/measurement methodology and no defined success criterion for the headline result: the per-timestep comparisons report only 'median of N runs' with N, warm-up, and clock-pinning left as TODOs, no variance/CI/run-count is specified, and the flagship 'automatically recovers the hand-tuned gain' claim has its threshold written literally as 'recover >=X% of the manual gain' with X unfilled.
  - *Why:* A hostile PC for a wall-clock-speedup paper demands run counts, dispersion, and a pre-registered success bar; reporting bare medians on GH200/MI300A (which have well-known clock/thermal variability) without CIs invites the 'is this noise?' rejection, and an undefined X means the paper's central success claim currently cannot be evaluated as pass or fail even in principle.
  - *Fix:* Specify the measurement protocol (locked clocks, warm-up discarded, N>=10, report median with IQR or a bootstrap CI, geomean across cases with its own CI) and commit to a concrete recovery threshold (e.g. 'recovers >=90% of the manual speedup') before the cluster runs, so the result table lands with dispersion and a stated bar rather than point estimates.


## MAJOR

- **[answerable-now]** C1's promised technical core -- a sound may-write proof certifying glacial status across COMMON/EQUIVALENCE aliasing and pointer association in multi-TU Fortran 90 -- overlaps the published interprocedural pointer/alias analysis SFAC (Blazy, ASE 2000) already performs for Fortran 90 specialization, yet related.tex distinguishes SFAC only on goal (comprehension), value-level-only, and single-program output, never on the alias-soundness analysis that is exactly where C1 stakes its weight.
  - *Why:* After the PC concedes the neck-split to LMCAS and the 'glacial' name to Autrey-Wolfe, the one pillar giving C1 standalone technical content is the aliasing-sound glacial certification. But the most dangerous Fortran prior art (the brief itself flags SFAC as 'the one tool nearest C2/C3' and notes it has 'interprocedural pointer analysis + correctness proof') already published interprocedural alias analysis with a correctness proof for the same language family and the same constant-propagation-plus-IF-simplification pipeline. The related-work distinguishes SFAC on every axis except the soundness/alias machinery, so a reviewer can argue C1's remaining novel pillar is anticipated on the exact dimension the paper leaves uncompared.
  - *Fix:* In related.tex, directly compare C1's may-write/glacial certification against SFAC's interprocedural pointer analysis and correctness proof, and articulate the genuine delta (SFAC proves the specialization correct given hand-supplied constants and aliasing facts within one program; C1 must additionally prove set-once-read-many GLACIALITY across an init/compute phase split and across the I/O barrier in a multi-TU build, which SFAC never addresses). Desk-level once the recovery.tex theorem placeholder is actually written; if the theorem is never written the pillar is unsupportable.

- **[needs-evaluation]** C2 (the explicitly designated headline/centerpiece) is only exercisable on ICON-O's polymorphic solver: the other two case studies have no finite-type-set / dynamic-dispatch story -- QE's vexx is a numerical exact-exchange kernel and ecRad is loop-bound/spectral (#1/#3) -- so the central contribution is evaluated on a single application and a single fixture-derived dispatch site, while the multi-application breadth (ICON+ecRad+QE x GH200+MI300A) decorates contributions C1/C3, not C2.
  - *Why:* After the brief re-ranks defensibility to C2>C3>C1 and the intro names C2 'our centerpiece,' a PC will expect the headline mechanism to generalize across the three flagship codes; a centerpiece demonstrated on exactly one real application (plus a toy poly.f90 fixture) is a generality gap precisely where the paper is staking its novelty, and the three-application setup risks being read as breadth-washing that does not touch the lead claim.
  - *Fix:* Either surface and devirtualize at least one more real config-selected polymorphic dispatch site outside ICON-O (e.g. a QE or ecRad solver/scheme-selection class hierarchy) and report dispatch-count->0 plus speedup there, or explicitly retitle/reposition so the headline is the broadly-evaluated contribution and C2 is scoped as 'demonstrated on ICON-O.' Adding a second real devirt site requires cluster runs and frontend coverage, so it is an evaluation task, not a desk fix.

- **[framing-only]** C2's centerpiece claim that devirtualization 'enables lowering at all' / is 'a reachability result parallel to C1' is an artifact of the DaCe Fortran frontend lacking an SDFG node for fir.dispatch, not a property of the optimization, of Fortran, or of any production compiler.
  - *Why:* The paper's own devirt_wpd experiment shows flang lowers fir.dispatch to a perfectly working indirect call and nvfortran -O4 emits a working callq *(%rax); the solver is 'unlowerable' only in one immature research frontend. Equating a missing IR node in DaCe with C1's genuine I/O-barrier+linkage structural barrier (which the LU experiment proves holds for the entire LLVM pipeline) overstates the result, and a CGO PC will read 'parallel to C1' as conflating a tooling gap with a fundamental one. This is the headline contribution, so the inflation is load-bearing.
  - *Fix:* Demote the reachability framing to a frontend-engineering observation: state plainly that DaCe currently has no fir.dispatch lowering, so devirt is a prerequisite for analysis *in this substrate*, and that in a mature LLVM/Flang backend the dispatch lowers fine and C2 is purely an optimization. The implementation.tex todo already half-concedes this ('sharpest in the dataflow backend; in an LLVM backend C2 reverts to value->type dispatch elimination') -- promote that concession out of the footnote and stop calling it 'parallel to C1'.

- **[answerable-now]** C2's claimed novelty is the abstraction ("derive a type-set from a recovered value"), but the underlying mechanism — propagate an integer tag's value-set, apply the program's select-type/factory discriminator delta to obtain the reachable concrete-type set, then prune the dispatch chain — is structurally a standard constant-folded SELECT TYPE / tagged-union discriminator narrowing, which constant propagation across a select-type already does, and 'the set of concrete types reachable at a call site' is exactly what CPA (Agesen 1995) and speculative-devirt JITs abstract.
  - *Why:* A sharp PC will argue C2 is a new value source (config file) feeding an existing analysis pattern (tag-driven select-type folding), i.e. the same 'refinement not fresh mechanism' concession the authors already make for C1 — undercutting the claim that C2 is a 'genuinely new mechanism' and the cleanest contribution.
  - *Fix:* Make the mechanism delta explicit: state precisely what standard select-type-folding cannot do (recover the tag value across the I/O barrier from module state, and handle dispatch on a CLASS(*) field never compared in a local type-is guard), and cite CPA/speculative-devirt as the type-set-component precedent (per the brief's G.0b R2) while claiming only the value->type reduction rho as new. Reframe C2 as 'new driver for an existing dispatch-elimination pattern,' parallel to the C1 concession, rather than a brand-new mechanism.

- **[needs-evaluation]** C2's devirtualization soundness requires proving the dispatched object's DYNAMIC TYPE (the type tag) is glacial across the object's lifetime from allocation to the dispatch site, but the paper's may-write analysis and the rho reduction are framed entirely on VALUE storage; nothing certifies the type tag itself is set-once, and Fortran provides several legal ways to change an allocatable's dynamic type after creation (re-ALLOCATE after DEALLOCATE, MOVE_ALLOC from a different concrete source, sourced ALLOCATE(s, source=...), pointer reassociation through INTENT holes).
  - *Why:* The poly.f90 fixture has exactly one allocate immediately above the dispatch, which hides this obligation, but a real ICON-O solver object is allocated, possibly re-selected across procedure boundaries, and dispatched in a loop; devirtualizing to a single concrete arm is unsound if any reachable path between allocation and dispatch can change the dynamic type, and this type-tag may-write proof is distinct from both the value-storage may-write (C1) and the closed-world discriminator-construction problem -- it is currently neither stated nor discharged.
  - *Fix:* Add a glacial-certification obligation specifically on the dynamic-type tag (no reachable ALLOCATE/MOVE_ALLOC/sourced-allocate/pointer-reassoc between the type-fixing site and the dispatch), and exercise it on the real ICON-O solver -- not the fixture -- to show the obligation actually discharges on a production object allocated and dispatched across TU boundaries; this needs the ICON-O run, not just a desk argument.

- **[answerable-now]** C2's related work omits profile-guided indirect-call promotion (ICP) / speculative devirtualization (Calder; ICC; LLVM -fprofile-guided; HotSpot/JVM), which is the single most mechanically-similar published transformation: ICP narrows an indirect call to its dominant concrete callee and emits exactly the `if (type==T) direct_call() else fallback` form C2 synthesizes from its recovered type-set.
  - *Why:* A novelty/prior-art reviewer judges C2 at the transformation level, and C2's emitted artifact (a guarded/pruned if-chain over concrete types replacing a dispatch) is literally the ICP code shape. The related-work section frames the only devirt competitors as CHA, LLVM whole-program-devirt, and Julia-JIT, and distinguishes them solely on 'they get the callee set from type metadata or runtime types, we get it from a recovered value.' But ICP also derives the callee from a value source other than the type hierarchy (a profile), and is a static AOT pass in production compilers, directly undercutting the paper's repeated 'no AOT pipeline performs this' positioning on the transformation axis. Leaving the closest mechanism uncited reads as an incomplete prior-art survey on the centerpiece contribution.
  - *Fix:* Add an ICP/speculative-devirtualization paragraph to related.tex (the brief's own G.1 #1 already lists it as a kill-search) and state the real delta precisely: ICP/CHA/JIT pick the callee from a runtime profile, observed runtime types, or the type hierarchy and must keep a guard or recompile; config-prop derives the type-set by static abstract interpretation over config-fixed values and deploys the pruned site unguarded. Desk-level writing and citation fix, not new measurement.

- **[answerable-now]** C2's soundness rests on the discriminator delta_x: Z -> T_x being total and closed, but the type-set it produces is open under Fortran's separately-compiled type extension: a deployment can link a solver subtype defined in a module not present at analysis time (or reachable via a generic/abstract-factory site), so the recovered finite type-set can miss a concrete type the running binary actually allocates, and the devirtualized if-chain has no arm for it.
  - *Why:* If the analysis prunes the dispatch chain to a type-set that is not actually closed over the deployment's linked subtypes, the specialized binary will mis-dispatch (or hit no arm) for a configuration that selects an unseen extension type -- a silent-miscompute / wrong-callee outcome, not a graceful fallback. This is the soundness counterpart to the closed-world precondition the paper criticizes CHA for, and it bites specifically because Fortran type extension and the factory site can live in TUs the whole-program view does not see when the dispatch target is itself resolved late.
  - *Fix:* State the closed-type-universe precondition explicitly as part of C2's soundness condition (every concrete type extending the dispatched class must be visible in the whole-program view and reachable only through the enumerated factory sites), and specify the refusal behavior when the type lattice cannot be closed (keep the runtime vtable dispatch / build the union if-chain with a default arm that calls the runtime-resolved binding). Tie this to the existing closed-world discussion so it is a one-paragraph hardening rather than a new mechanism.

- **[framing-only]** C3's actual deployment mechanism -- 'name-based startup dispatch' that selects one of several pre-built unguarded whole-program binaries by which config cell the namelist inhabits -- is mechanically the established function-multiversioning / GNU IFUNC / fat-binary (glibc hwcaps, target_clones, CPU-feature multiversioned dispatch) primitive, just keyed on a parsed config file instead of CPU capability bits, yet the 'Selecting a set of variants' related-work paragraph compares only to runtime-classifier autotuning and GPU multiversioning and never to the loader-level static-dispatch-among-prebuilt-variants primitive that C3's deployment actually is.
  - *Why:* C3 is positioned as a systems contribution resting partly on the 'unguarded, dispatched once at startup' deployment model; a PC who has shipped multiversioned libraries will see that the deploy-side mechanism is off-the-shelf IFUNC-style fat-binary dispatch with the selector swapped, so the residual novelty narrows to 'pick the variant set by greedy coverage over a namelist distribution' -- exactly the part the authors already concede is SPL/covering-array-adjacent.
  - *Fix:* Cite function-multiversioning/IFUNC/fat-binary dispatch explicitly as the deployment-mechanism precedent, state that C3 reuses it with a config-derived selector, and make clear the claimed contribution is the variant-SET-selection objective (coverage over a namelist distribution) and not the startup-dispatch mechanism itself.

- **[answerable-now]** C3's algorithm is named 'greedy minimal-covering-set / set cover,' but the actual problem — choose at most K unguarded binaries to maximize performance-weighted coverage over a namelist distribution, with a generic fallback binary always available — is soft-coverage maximization (max-coverage / facility-location / k-median), not classical minimum set cover, which requires every element be hard-covered and minimizes set count with no performance objective.
  - *Why:* Mis-naming the core algorithm is a correctness-of-framing red flag for a CGO PC: set cover minimizes the number of sets needed to cover ALL elements, but here a fallback makes full coverage trivial and the real objective is a budget-bounded performance gain — so 'greedy set cover' is the wrong template and a reviewer will question whether the authors understand their own optimization problem and whether the greedy approximation guarantee they implicitly invoke even applies.
  - *Fix:* Restate the problem formally: given namelist distribution D, per-cell specialized speedup, a generic fallback, and binary budget K, maximize expected speedup — recognize this as weighted maximum coverage / k-median (submodular, 1-1/e greedy guarantee), not minimum set cover. Cite the correct approximation result and distinguish from SPL t-wise covering arrays (test coverage, hard cover) per the brief's T3 hedge. If hard-coverage minimum-set-cover is genuinely intended, justify why no generic fallback is allowed.

- **[needs-evaluation]** No measurement anywhere -- not in the experiments, not promised in the eval -- establishes that the config-guarded branches actually sit on the runtime-hot path of the production codes at meaningful magnitude; Listing 1 and the LU loops are asserted 'memory-bandwidth-bound' and hot, but the paper gives no profile, FLOP/byte count, or fraction-of-runtime attribution for the specialized regions.
  - *Why:* The headline framing -- 'a tax paid on every timestep' the contributions remove -- presupposes the affected code dominates runtime; without a baseline profile showing these kernels are a large share of wall-clock, even a correct speedup on the specialized region (e.g. velocity_tendencies over only nlev column points) can be negligible end-to-end, and Amdahl's law is the first thing a CGO PC checks on a 'we make X faster' systems paper.
  - *Fix:* Add a per-application hot-path profile (fraction of timestep wall-clock in the config-guarded/specialized regions) as the denominator for the end-to-end claim; a coarse CPU profile of the case studies could establish it at the desk, full attribution needs the GH200/MI300A runs.

- **[needs-evaluation]** The 'deployment-time specialization as a distinct third lifecycle point (AOT/JIT/deployment-time)' framing suppresses that every covered configuration is produced by a full from-source rebuild of an unguarded whole-program binary -- i.e. ordinary AOT performed once per namelist cell -- so the 'pays zero runtime cost' pitch silently trades a per-deployment, per-variant build cost (build-time x covering-set size, binary-size blowup) that the cost model never reconciles against the AOT it claims to transcend.
  - *Why:* The lifecycle figure is the paper's conceptual home, but a sharp PC will note that 'know the values, rebuild, name-dispatch' is just AOT specialization keyed on the config file, and that the only thing distinguishing it from running the compiler per deployment is the covering-set bookkeeping; if the covered set is large or namelists are diverse, the build/storage cost can dominate, and presenting 'zero runtime cost' without the amortization story overstates the win and weakens the claim that this is a new lifecycle phase rather than per-deployment AOT.
  - *Fix:* Add an honest cost accounting to RQ5: per-variant build time, total covering-set build time and binary footprint vs. coverage, and the amortization break-even (timesteps-per-deployment needed for the per-run savings to repay the one-time rebuild), and reframe the lifecycle pitch as 'AOT specialization driven by the config file, amortized over a long glacial run' rather than a cost-free new phase. The qualitative reframe is desk-time; the break-even/footprint numbers require the cluster builds.

- **[answerable-now]** The LU README presents the I/O barrier evidence as the gfortran symbol `_gfortran_transfer_integer` (line 34), but the entire experiment is compiled with flang-new-21 (reproduce.sh line 8), whose namelist runtime emits `_Fortran*`/`_FortranANamelist` calls, not `_gfortran_*`; the cited symbol appears nowhere in the produced IR and belongs to a compiler the experiment does not use.
  - *Why:* It signals the central 'opaque runtime call past which const-prop must assume anything' claim was never observed in the artifact's own IR -- the load-bearing mechanism is named from a different toolchain, which a careful PC reading the artifact will flag as evidence the barrier was assumed rather than measured.
  - *Fix:* Compile the namelist-read variant with the toolchain actually used and quote the real namelist-runtime symbol from the produced .ll (flang's `_FortranANamelistRead` or gfortran's `_gfortran_transfer_integer`), and correct the README to match.

- **[answerable-now]** The LU experiment models config state as bare module scalars (INTEGER nz0), but the paper's own background says ICON deposits namelist values into 'a field of a derived type in a configuration module' (mo_run_config etc.). Module-scalar folding and derived-type-field folding differ materially under LLVM in aliasing, field-sensitivity, and internalization, so the experiment may not reproduce the real obstruction it claims to characterize.
  - *Why:* The 'why -O3+LTO cannot fold it' argument is the load-bearing motivation, demonstrated on a layout that does not match what the paper itself says production code uses; a reviewer who knows the internals will ask whether row C still reaches 0 loads when nz is a field of a SAVEd module derived-type reached via USE across TUs, and either answer breaks the experiment-to-claim link.
  - *Fix:* Re-run the three-row LU experiment with nz held in a module derived-type field matching the cited mo_*_config layout and report whether linkage/internalization behavior changes; also add the missing gfortran -O3 -flto and nvfortran columns (reproduce.sh only runs flang/opt; the nvfortran artifacts are hand-produced and unscripted) so 'no production compiler folds it' is actually backed. Desk-doable with existing compilers.

- **[answerable-now]** The LU experiment's positive control (row C) is constructed as the exact OPPOSITE of the production phase-separation scenario the paper says is fundamental: the injected value is a compile-time literal STORE (nz0 = 33) placed at the top of dolu(), and main calls dolu() which then runs the whole compute phase (ssor) in the same closed unit, so GlobalOpt does not merely 'fold a supplied scalar' -- it SROAs the global away entirely (verified: 0 surviving loads AND 0 stores of @nz0/@nz), which only happens because init and compute share one from-source closure.
  - *Why:* The paper leans on row C as the 'strongest standard-tool baseline' and as proof the gap is structural, but row C's success is an artifact of collapsing the init/compute phase boundary the paper claims production codes never collapse; the table's own positive control thus silently contradicts the central phase-separation thesis instead of isolating 'value supplied to a closed program.'
  - *Fix:* Reconstruct row C to preserve the phase boundary: place the literal nz0 store in a separately-compiled init TU not reachable in the same source closure as the compute (or behind an opaque call). If folding then fails, that is the real structural result; if it still succeeds, the structural claim must be re-scoped to pure linkage closure.

- **[framing-only]** The LU experiment's three rows actually show the scalar case is NOT structural: row C folds the global to 0 loads once the value is present and the program is closed under LTO, so the barrier is the well-known single-TU external-linkage limitation (row B) plus mere value-absence (row A), neither fundamental nor novel; and the paper's own RQ4 strongest baseline (recovered values injected as constants under whole-program LTO) IS row C, so the table simultaneously motivates the structural claim and concedes that the strongest standard baseline closes the scalar gap.
  - *Why:* The abstract and intro lean hard on 'not a missing optimization but a structural one,' yet the experiment shows the scalar configuration tax is fully recoverable by value-injection plus LTO internalization, exactly what the RQ4 baseline does; a PC could read the central structural claim as contradicted by the authors' own baseline for the scalar case.
  - *Fix:* Reframe RQ1 so 'structural' attaches only to the value-across-the-I/O-barrier step (row A) and to the optimizations row C cannot perform (devirt, index narrowing); explicitly state the scalar cascade is standard once the value is supplied (row C). Stop calling the scalar case structural. Pure positioning fix, no new runs.

- **[answerable-now]** The README cites '731 literal 33s' in lto_O3.ll as positive proof that the value propagated, but counting textual occurrences of the constant is not a folding metric: in this file the 33s are overwhelmingly operands of the runtime `select i1 %flag, i32 33, i32 0` reconstructions and of array-stride arithmetic (`mul i64 %3, 43560`), i.e. the value appears in the IR precisely as the not-yet-folded arm of a runtime branch, so the count rises whether or not folding succeeded.
  - *Why:* A CGO PC scrutinizing the artifact will see the paper offer two proxies for the same claim (load-count and literal-count) that both fail to distinguish 'folded into a constant bound' from 'rematerialized by a runtime select', and the PC has already declared static codegen proxies insufficient; presenting a confounded proxy as the proof of the headline structural-tax result invites rejection of the experiment's validity.
  - *Fix:* Replace the literal-occurrence count with a check that the loop trip counts and array bounds are compile-time constants (e.g. no phi/select feeding the canonical induction variable's bound, or a runtime instruction-count / branch-count delta), and report that quantity for rows A/B/C instead of grepping for the digits '33'.

- **[answerable-now]** The claimed C1 delta over LMCAS — 'recovery of persisted module-level state that survives across the compute phase' — is a target-language difference (Fortran module globals vs C CLI args/globals), not a new analysis mechanism; the one genuinely new mechanism that would carry technical weight, the sound glacial-certification may-write proof across the compute phase (handling COMMON/EQUIVALENCE aliasing, pointer association, INTENT(IN) holes), is currently only an unwritten placeholder in recovery.tex.
  - *Why:* The paper already concedes the neck-split to LMCAS and the name to Autrey-Wolfe, leaving the glacial-proof as C1's only technical content; if that proof is absent and the stated delta reduces to 'we handle Fortran modules,' a PC will judge C1 as a port of LMCAS to a new language rather than a contribution, and the 'structurally cannot reach' claims about LMCAS (which already partial-interprets state reachable at the neck, including globals) become hard to defend.
  - *Fix:* Write the glacial-certification theorem and soundness argument as the actual C1 mechanism, showing concretely what LMCAS's neck-based partial interpretation cannot certify (e.g. that a module field is never written in ANY reachable compute-phase path across hundreds of TUs without running to a neck, under Fortran aliasing semantics) — a static may-write/aliasing result LMCAS's dynamic interpretation-to-neck does not provide. If after writing it the delta is still only 'module vs CLI args,' demote C1 to a refinement and lean on C2/C3.

- **[answerable-now]** The devirt_wpd experiment tests only the three things that trivially fail (flang rejects -fwhole-program-vtables, emits zero !type metadata, nvfortran leaves an indirect call) but never runs flang's OWN -O3 -flto / FIR pipeline on poly.f90 to check whether flang devirtualizes fir.dispatch through its dispatch-table machinery once the allocate is constant-folded; the artifact directory holds only nvfortran asm (poly_nv.s) and no flang optimized .ll/.s for the polymorphic path.
  - *Why:* C2 is the designated headline and its strongest framing ('no production Fortran toolchain performs this optimization') rests on an experiment that demonstrably did not exercise the one path that could falsify it -- flang's own FIR optimization of a locally-constant-folded allocate into a direct call; the blanket conclusion is inferred from absence of a C++-only flag, not from flang's actual output.
  - *Fix:* Run flang-new -O3 -flto (and the relevant FIR passes) on poly.f90, capture the optimized FIR/LLVM IR, and report whether the dispatch survives after the sel==1 literal folds; commit that artifact next to poly_nv.s and state the observed result rather than inferring it from flag rejection.

- **[answerable-now]** The devirt_wpd nvfortran evidence (poly_nv.s) was produced by compiling poly.f90 alone -- the file contains only solvers_* symbols, no main/MAIN -- so inside the exported procedure solvers_run_ the selector `sel` is an opaque formal argument and the concrete type is provably NOT statically determinable in that translation unit; the observed indirect `callq *(%rax)` is the trivially-expected result for an exported function with an unknown integer arg, not evidence about a case where the type is known.
  - *Why:* The README's and specialization.tex/RQ4's headline -- 'no production Fortran toolchain devirtualizes even when the dynamic type is statically determinable' -- is the empirical backbone of the C2 'no production toolchain does this' claim, yet the only nvfortran artifact never placed the compiler in the determinable case (it never saw the driver's literal sel=1), so the strongest clause of the claim is untested by the artifact the paper cites.
  - *Fix:* Recompile driver.f90+poly.f90 together (or with -Mipa-equivalent IPO / forced inlining of run into main) so nvfortran/flang see sel=1 as a literal, and report whether the dispatch is still indirect; if it is, the claim is supported; if it is not, narrow the claim to 'across translation-unit boundaries' and drop 'even when statically determinable'.

- **[answerable-now]** The devirtualization fixture poly.f90 makes the standard toolchain's failure unsurprising for the wrong reason and does not model the cross-TU, module-state-driven dispatch the paper says is the hard case: run(sel,x) allocates the concrete type via a local IF(sel==1) immediately above the dispatch s%solve(x) in the SAME function, and the driver calls run(1,3.14) with sel a literal, so after inlining/SROA the dynamic type is locally and trivially knowable, exactly the case an aggressive C++-style pipeline DOES devirtualize, yet the README still reports 'no Fortran toolchain devirtualizes.'
  - *Why:* C2 is the declared headline contribution and its 'standard path structurally cannot do this' claim rests entirely on this fixture; a rigged-easy fixture undermines the centerpiece.
  - *Fix:* Add a second fixture where the selector enters via a module variable initialized from a namelist READ in a separate TU (matching the paper's stated ICON pattern), and a third where the type is statically known but still cross-TU; show flang/nvfortran leave the indirect call in BOTH the easy and hard cases, establishing that the Fortran path lacks the pass entirely independent of type-knowability. Desk-doable with the existing toolchains.

- **[framing-only]** The ecRad impact anchor — the paper's only quantitative 'what we automate' number — credits config-propagation with contributing to a 3x 'code-optimization' factor that the cited source (Ukkonen & Hogan, per the brief's own G.0b fidelity correction) attributes to a bundle of single-precision porting + loop restructuring + g-point hardcoding, with no separable measurement isolating the g-point/config-flag part, so the claimed automatable factor is unquantified and confounded.
  - *Why:* A CGO PC reading the cited JAMES paper will see the 3x is explicitly 'for both solvers' as combined code optimization (single precision + restructuring), and the dominant residual factor is spectral reduction; the paper leans on this as its headline existence-proof of real-world impact, yet cannot point to any number that is the config-propagation contribution alone — the impact claim rests on a factor it cannot attribute to itself.
  - *Fix:* Drop any phrasing that implies a numeric share of the 3x and state only that g-point hardcoding is one named hand optimization inside an aggregate factor; better, replace the anchor with the paper's own measured automated-vs-manual recovery number once RQ3's 'recover >=X% of the manual gain' experiment exists, so the impact claim is carried by a config-prop-attributable measurement rather than a confounded external factor.

- **[hard-to-answer]** The entire C3 'variant economy' empirical claim depends on a probability-weighted distribution D of real operational namelists that the authors admit they do not have: RQ5/F3 says obtaining operational decks 'REQUIRES' DKRZ/ECMWF access and falls back to mining in-tree icon-model/run/ example namelists, which are developer test/tutorial decks, not operational deployments, and carry no deployment-frequency weights for the runtime-cost objective the greedy set-cover optimizes.
  - *Why:* C3's novelty is explicitly the performance-objective covering set over an operational-namelist distribution; if the only available distribution is unweighted in-tree example decks, the binary-count-vs-coverage curve measures coverage over a synthetic, possibly unrepresentative set, and the 'we bound binary count to what centers actually deploy' claim is unsupported — the one thing that distinguishes C3 from prior SPL set-cover is the data the paper does not yet have.
  - *Fix:* Either secure real operational decks with deployment frequencies (the genuinely convincing path) or, if only in-tree namelists are available, reframe C3 honestly as a methodology + worked example on an illustrative distribution, drop the probability-weighted-objective language, and add a sensitivity analysis showing how the covering set changes under plausible reweightings so the result is not silently tied to one unrepresentative deck set.

- **[framing-only]** The entire paper is implemented and evaluated only on the DaCe SDFG substrate, yet implementation.tex and the C2 boundary notes repeatedly assert the technique 'is not DaCe-specific,' 'belongs at the FIR/HLFIR level,' and is 'a natural Flang upstream' / 'IPSCCP-seeding pass' -- none of which is built, prototyped, or measured; the generality of the whole approach rests on unbuilt portability promises.
  - *Why:* A CGO PC reads 'general compiler technique' as a load-bearing claim; if every result, every speedup, and the only working pipeline live in one research dataflow compiler with a Fortran frontend of uncertain coverage, the contribution may be a property of DaCe's whole-program SDFG view rather than of configuration-guided optimization, and the paper's repeated 'could integrate as a Flang FIR pass' hedges concede this without discharging it.
  - *Fix:* Either (a) build the minimal FIR/HLFIR-level prototype the impl section keeps promising -- at least the C1 IPSCCP-seeding pass and the value->type fold on fir.dispatch -- and show one number from it, converting the portability claim into evidence; or (b) demote every 'not DaCe-specific'/'natural Flang upstream' sentence to an explicit, clearly-scoped Future Work / Limitations item and confine all main-text claims to 'in a whole-program dataflow compiler.' Without code this can only be a writing fix, and a strong PC will treat the unbuilt version as conjecture.

- **[hard-to-answer]** The entire two-stage framing assumes a clean init/compute split with the namelist READ as the staging boundary, but the paper never says how the analysis LOCALIZES that boundary, and in real codes the glacial fixpoint is reached long after the read (config derivation, MPI broadcast/scatter of config to ranks, domain decomposition, resolution-dependent re-defaulting, restart/checkpoint override of namelist values), so 'the I/O barrier is the staging boundary' is false as stated.
  - *Why:* Binding-time-analysis boundary localization is the actual technical content the may-write proof presupposes -- if the boundary is mis-placed at the READ, the analysis will (a) certify a variable glacial while a legitimate later init-phase store still mutates it, or (b) refuse everything because writes appear 'after' the READ; either way C1's certification theorem is unstated precisely where it is hardest, and the DEEP-ARG-C 'automatic binding-time analysis that locates the boundary' is an unwritten placeholder doing the load-bearing work.
  - *Fix:* Define the staging boundary as the last program point dominating all timestep-loop entries such that the compute-phase may-write set for a candidate variable is empty, rather than the textual READ; prove it is well-defined (or admit per-variable boundaries) and handle MPI broadcast as a copy edge, restart as an alternative writer that forces refusal -- this is genuine analysis design work, not a desk edit.

- **[framing-only]** The fall-back safety net that guarantees 'never silent miscompute' (eval overhead) presupposes a correct generic binary to fall back to, but C2's own centerpiece claim is that the polymorphic-dispatch solver is unlowerable in the dataflow backend without devirtualization -- so for exactly the C2 case there is no generic binary to land on when a deployed namelist misses every covering-set cell.
  - *Why:* A soundness-skeptic PC will see a direct contradiction between the paper's stated safety guarantee (any unmatched config falls back to a generic build, so deployment is never unsound) and its headline reachability claim (no generic, runtime-valued polymorphic build exists in the dataflow substrate). If the generic fallback cannot be built for the dispatch case, the unguarded multi-binary scheme has no safe default precisely where the centerpiece optimization applies, undermining the 'unguarded but always safe' pitch that ties C1/C2/C3 together.
  - *Fix:* Resolve the contradiction in text: either (a) concede the generic fallback for the dispatch case must itself be a devirtualized union-of-all-reachable-types binary (i.e., the 'generic' build is a guarded if-chain over the full type-set, not a runtime-vtable build), and state how it is produced; or (b) scope the safety guarantee to scalar/interval cases and state explicitly that the type-set case requires the covering set to be exhaustive over the closed type universe, with refusal (build failure) rather than runtime fallback when it is not.

- **[needs-evaluation]** The float bit-reproducibility argument (eval overhead section) claims specializing on an exact namelist float is bit-identical 'to a build compiled with that literal when FP-contraction flags are held fixed', but that is the wrong baseline: the production run computes the expression from a runtime-loaded float, and replacing the load with a literal lets the compiler constant-fold subexpressions, reassociate, and form or break FMAs differently at the same -ffp-contract level, so the specialized binary can differ in the last bits from the binary the center actually runs today.
  - *Why:* The PC explicitly cares about correctness for production deployment; an unguarded last-bit change to an operational forecast model is a credibility and adoption blocker, and the current claim is verified against a strawman baseline.
  - *Fix:* Run the bit-for-bit check the right way on ecRad: diff the specialized binary's output against the UNSPECIALIZED runtime-valued production binary (not against a literal-compiled build), under the operational FP flags, and report whether any float-config specialization changes results; if it does, either restrict float specialization to values that are provably round-trip/FMA-neutral or expose it behind an opt-in flag. This needs the actual ecRad numerical runs.

- **[needs-evaluation]** The float-config bit-reproducibility claim (evaluation.tex: specializing on an exact namelist float 'is bit-identical to a build compiled with that literal when FP-contraction flags are held fixed') is asserted as a property but never proven, and is not obviously true: injecting a float constant lets the optimizer constant-fold FP subexpressions at compile time (different rounding/association than the runtime-valued path), so bit-identity can fail even with contraction flags fixed.
  - *Why:* For climate/weather/materials reviewers, silent numerical drift from a build-time specialization is a first-class correctness concern; an unproven bit-reproducibility assertion on the one float case study (ecRad coefficients) is exactly the kind of soundness gap a hostile PC will demand evidence for, and the paper currently only promises to 'verify bit-for-bit on ecRad' rather than showing it.
  - *Fix:* Produce the bit-for-bit ecRad comparison the section promises, AND state precisely which compile-time FP foldings the specialization enables vs. the literal-build reference (they should be identical IR, so the claim reduces to 'same IR => same bits' — make that argument explicit rather than asserting bit-identity as given).

- **[answerable-now]** The formal core of C2 -- the value->type reduction rho mapping each discriminator tag value to the concrete type a SELECT TYPE/factory allocates (approach.tex's delta_x: Z -> T_x) -- is structurally identical to published flow-typing / occurrence-typing / tagged-discriminated-union narrowing (Typed Racket occurrence typing, TypeScript/Flow control-flow type narrowing on a discriminant, GADT tag-to-type refinement), which is exactly 'given a constant tag value, refine to the single concrete type and specialize the branch'; related.tex frames C2's novelty only against CHA/WPD/Julia and never against this type-theory line that names the precise rho map.
  - *Why:* C2 is the explicitly designated headline/centerpiece and its sole formal claim is the value->type lift; a PC from PL/types will immediately recognize discriminant-tag-to-type refinement as a textbook flow-typing operation, so 'no prior specializer derives a type from a recovered value' reads as unfamiliarity with the occurrence-typing literature rather than a real gap, undercutting the centerpiece at the mechanism level.
  - *Fix:* Survey occurrence/flow typing and discriminated-union narrowing in related.tex, then sharpen the genuinely-new delta if one survives (e.g., the tag value is recovered statically from a config FILE across an I/O barrier and drives whole-program devirtualization+lowering, vs. local intra-procedural narrowing of an in-scope discriminant) -- or downgrade the 'first to derive a type-set from a value' claim.

- **[answerable-now]** The glacial may-write certification (recovery.tex) treats the namelist READ as the one-time staging boundary and audits only data-memory stores in the compute phase, but it omits the I/O channel itself as a recurring write: Fortran legally permits a second READ(NML=...) into the same module storage during timestepping (assimilation/IAU increment updates, event-driven mtime re-reads, replay/nudging decks), which re-writes the 'glacial' variable through the runtime I/O path, not through a store the may-write analysis inspects.
  - *Why:* A variable the analysis declares glacial and bakes in as a constant can in fact be overwritten mid-run by a second namelist read, producing a silent miscompute -- exactly the failure mode the soundness theorem is supposed to exclude. The audited write channels (COMMON/EQUIVALENCE, pointers, INTENT(IN) holes) all assume the only I/O write is the initial staging read; the recurring-READ channel is a structural blind spot because the framing makes the I/O barrier the boundary rather than a possible in-phase writer.
  - *Fix:* Add to the may-write soundness condition an explicit clause that no READ/I/O transfer targeting the variable's storage is reachable in the compute phase (not just no assignment store), and state that any variable reachable by a second namelist/I/O read is refused. Note the concrete ICON cases (IAU/assimilation increment updates, mtime event re-reads) to show the channel is real, and confirm the analysis already models I/O calls as opaque writers so the audit is sound by construction.

- **[hard-to-answer]** The glacial may-write certification in recovery.tex enumerates COMMON/EQUIVALENCE aliasing, pointer association, INTENT(IN) holes, and conditional namelists, but omits write channels that exist in real ICON/QE builds (MPI receive buffers and one-sided/RMA targets aliasing config storage, C-interop reassociation via c_loc/c_f_pointer, and writes through procedure pointers / generic-interface targets resolved at runtime), and a sound whole-program may-write across a multi-hundred-TU separately-compiled build needs a callgraph that is itself incomplete precisely because of the indirect dispatch C2 is elsewhere said to leave opaque until devirtualized.
  - *Why:* The brief itself names this proof as the one mechanism giving C1 technical weight; leaving it unwritten with an incomplete alias set and an unaddressed circularity undercuts the contribution the paper most needs to defend on correctness grounds.
  - *Fix:* State the may-write soundness theorem with the FULL channel set (including MPI/RMA, C-interop, and indirect/procedure-pointer writes), make the conservative-refuse-on-unprovable fallback explicit, and address the callgraph-completeness/circularity by fixing a sound over-approximation (assume every indirect callee may write unless devirtualized first); then report the refusal rate on the real case studies. Writing the theorem is a desk task, but demonstrating it certifies a useful fraction of real config vars rather than refusing everything requires running the analysis on ICON/QE.

- **[answerable-now]** The load-count metric is taken on @_QMluEnz (the global nz), but the injected constant is nz0, and nz is only ever set inside domain() by the derived assignment nz = nz0; so row C's '0 loads of nz' requires LLVM to fold a two-hop chain (literal store to nz0, propagate nz0->33, prove the single nz=nz0 store makes nz constant, then SROA nz), meaning the experiment silently shows whole-program LTO already folds an init-DERIVED secondary config value, not just the namelist literal.
  - *Why:* This is the 'post-init derived value' case (e.g. nblks, nlev+1) the paper elsewhere treats as a distinctive recovery challenge for C1; row C is evidence the standard pipeline already handles such derivation once the program is closed, reducing the headline obstruction to closure/linkage and undercutting the claim that derived/transformed config values need C1's analysis.
  - *Fix:* Count loads of the directly-injected global nz0 (matching the namelist analog) and report that number, or explicitly acknowledge row C demonstrates LTO folds the nz0->nz derivation and re-scope C1's novelty to the cross-TU/linkage closure problem; do not let the metric global differ from the injected global without comment.

- **[framing-only]** The paper sells 'interval arithmetic on collections' as a unifying *analysis* contribution (a contribution bullet, a title option, the abstract's organizing device) spanning four domain kinds, yet by the authors' own admission three of the four residualization cases are off-the-shelf (singleton->constant is 'standard, be honest, cf. row C'; finite-set->cross-product is SPL-style enumeration; interval->bitwidth is Bitwise-style narrowing), and only the value->type reduction is new - so the 'one lattice drives all four' framing is a post-hoc packaging of four independently-known optimizations under a shared name, not a mechanism whose unification does analytical work.
  - *Why:* A framing-skeptic PC distinguishes a genuine unifying abstraction (where the shared lattice *enables* something none of the parts could do alone) from a presentational umbrella (where the four cases would each work identically if implemented as four separate passes). Nothing in approach.tex shows the cases interact through the product in a way that produces a result unavailable to four siblings - the only cross-case interaction is the value->type reduction $\rho$, which is C2 alone. Presenting the umbrella as a co-equal contribution risks the verdict 'three-quarters of your headline analysis is prior art with a new label'.
  - *Fix:* Demote 'interval arithmetic on collections' from a co-equal contribution to a presentational framing, OR demonstrate genuine unification - e.g. a worked case where the *product/reduction* derives a specialization no single-domain pass reaches (a type-set narrowed by an interval-recovered tag feeding a bound that then narrows an index type). If such a case exists in the ICON-O solver it is desk-describable; if not, the honest fix is to scope the claim down. Either way addressable in approach.tex/abstract without the cluster runs.

- **[framing-only]** The recovery maps each READ(NML=...) target to a module field and recovers the raw namelist value, but production codes routinely transform that value during initialization before the compute phase (e.g. ICON derives nblks=(n-1)/nproma+1, adds halo levels nlev+1, clips/defaults, or recomputes spectral counts), so the value the compute phase actually reads is the post-init derived value, not the namelist literal the analysis folds in.
  - *Why:* A CGO PC reviewing a specialization-for-production-Fortran paper will immediately ask whether the recovered constant is the value the hot loop actually sees; a wrong answer means specialized binaries silently produce different physics.
  - *Fix:* Tighten the recovery definition: the recoverable quantity is the value of the field at the init/compute boundary (after all init-phase derivation), obtained by partial-evaluating the init phase up to the boundary (an LMCAS-style neck), NOT the namelist literal; state this explicitly in recovery.tex and add the init-phase write set to the certification. This is a desk fix to the analysis specification, not a measurement, but it materially changes what 'recover the value' means.

- **[hard-to-answer]** The strongest-baseline (recovered values injected as constants under whole-program LTO) cannot independently bound C1's contribution because constructing that baseline already presupposes C1's recovery is correct (you must have the recovered constants to inject them), so 'our gain above the LTO-injected-constant baseline' isolates only C2+C3, leaving C1's own end-to-end value with no standard-tool comparator that does not already embed C1's output.
  - *Why:* RQ4 is sold as the rebuttal to 'this is just constant injection + LTO,' but the design measures everything except C1: the bridge that is the paper's claimed enabler has no baseline that excludes it, so a PC can argue C1's quantitative contribution is untested — the LTO baseline is downstream of C1, not an alternative to it, and the unwritten glacial-soundness proof (recovery.tex placeholder) means even C1's correctness is currently unestablished.
  - *Fix:* Acknowledge that the LTO-injected-constant baseline isolates C2+C3 only, and separately substantiate C1 by (a) writing the glacial may-write soundness theorem and (b) reporting recovery precision/recall on real namelists (how many config vars are correctly certified glacial vs. conservatively refused) rather than implying RQ4 validates C1.

- **[answerable-now]** The unguarded covering-set scheme relies on a startup 'namelist-vs-binary domain check' and 'name-based startup dispatch' to select the correct pre-built binary or fall back, but for float-valued config the dispatch key is a floating-point value parsed from a text deck: exact equality against the binary's baked-in literal is fragile under decimal->binary rounding and deck reformatting, while any tolerance band would admit a binary specialized on a DIFFERENT float than the deployment actually uses.
  - *Why:* This is the soundness hinge of the 'never silent miscompute' guarantee for C3, and it is independent of (and prior to) the output bit-reproducibility question: if the dispatch decision itself is ill-defined for float configs, the guard can route a deck to a binary specialized on a nearby-but-different coefficient (e.g. an ecRad float), silently changing results, or reject all decks and collapse the variant economy to the generic fallback; the paper specifies neither the key normalization nor the comparison tolerance.
  - *Fix:* Specify the dispatch key as the exact bit pattern of the parsed float (parse the deck with the same reader the binary was specialized from, compare IEEE-bit-equal, no tolerance), restrict covering-set membership to variables whose deck->binary value round-trips bit-exactly, and state that any float that does not round-trip is left runtime-valued (generic); add this to variants.tex and the overhead subsection -- a design/writing fix, no cluster needed.

- **[answerable-now]** The unguarded multi-binary scheme relies on a startup namelist-vs-binary domain check to guarantee 'fall back to generic, never silent miscompute', but a single deployed binary bakes in MANY glacial values (the whole cross-product cell plus all singletons folded by C1), while the startup dispatch is described as name-based selection on the covering-set dispatch key; if the deployed namelist matches the dispatch key but differs in any secondarily-folded glacial variable (a conditional/secondary namelist, a default that changed across model versions, a co-folded singleton), the guard admits a binary specialized on a value the namelist does not actually have.
  - *Why:* A soundness-focused PC will probe precisely the unguarded-deployment safety claim; if the guard only checks the dispatch key it is a real silent-miscompute hole in the system contribution the paper most wants credit for.
  - *Fix:* Specify the startup guard as validating the binary's full recovered-domain witness (every folded glacial value and interval, not just the dispatch key) against the freshly parsed deployment namelist, and refuse-to-generic on any mismatch; state and prove this completeness property in the variants section. This is a design/specification fix doable at the desk, and it can be demonstrated cheaply by constructing a near-miss namelist that shares the dispatch key but differs in a co-folded variable.

- **[framing-only]** The unifying formalism is the *reduced product* of per-variable lattices ($\prod^\sharp_x \mathcal C_{k_x}(B_x)$) and the variant economy enumerates the *cross-product* of value-sets, but both are non-relational: they assume config variables vary independently, whereas real namelists carry strong inter-variable constraints (a solver selection forecloses certain grid/precision options; a `.false.` toggle makes a dependent block's namelist fields meaningless; derived defaults couple variables), so the abstraction over-approximates the deployable configuration space.
  - *Why:* This is a soundness-vs-coverage hole at the heart of C2/C3: an unguarded cross-product binary can be built for a (solver,grid) combination that no valid namelist can ever produce (wasting binaries and inflating the covering-set count C3 optimizes), while the per-variable type-set reduction $\rho$ can admit concrete types that a *correlated* second config variable actually excludes - a relational analysis would prune them. A CGO PC versed in abstract interpretation will immediately note that a relational/reduced-with-constraints domain is the technically interesting case and that the paper picks the trivially-sound but imprecise non-relational product without discussing the relational alternative.
  - *Fix:* Add a paragraph in approach.tex acknowledging the analysis is non-relational and stating that namelist inter-variable constraints are handled (if at all) only by post-hoc filtering of the cross-product against observed/valid decks, not by the lattice; alternatively cite that a relational completion (e.g. a constraint over the product) is future work. This is a writing/scoping fix unless they actually claim relational precision, in which case it needs the eval.

- **[answerable-now]** The unifying lattice 'interval arithmetic on collections' is, by the authors' own admission in approach.tex and the brief, an instance of the reduced product of standard abstract domains; the only genuinely new component is the value->type reduction rho, yet the paper sells the lattice itself as a contribution and even puts it in the title options.
  - *Why:* A CGO PC steeped in abstract interpretation will recognize that a reduced product combining an interval domain, a finite value-set (bounded disjunctive completion), and a flat type-set is textbook Cousot-Cousot machinery; three of the four 'cases' drive standard optimizations (const-prop, DCE, bitwidth narrowing a la Bitwise). Branding the packaging as a named unifying analysis invites a 'this is just a reduced product with a marketing name' rebuttal that the authors anticipate ('pre-empt the just-reduced-product rebuttal') but have not yet defused. The title-level prominence makes the over-claim conspicuous.
  - *Fix:* Reframe the lattice explicitly as a reduced (or Granger) product of known domains -- the brief already resolves to cite Cousot-Cousot 1979 and Agesen's CPA -- and relocate the actual contribution to the value->type reduction rho and the residualization map R that ties each domain case to a concrete specialization. Drop the lattice from the title; present 'interval arithmetic on collections' as a convenient framing whose only new content is rho, not a novel abstraction.

- **[answerable-now]** The value->type reduction rho (the formal core of C2 per approach.tex) is defined only via the program discriminator delta_x: Z -> T_x mapping each tag value to the concrete type the factory/SELECT TYPE allocates, but constructing delta soundly requires seeing every allocate/factory site that can feed the dispatch -- i.e. a closed-world, class-hierarchy-style whole-program type-flow analysis, the exact precondition the paper criticizes CHA for needing.
  - *Why:* The paper sells C2 as 'inverting the direction' of CHA/WPD -- recovering the type from a value rather than from the hierarchy. But rho cannot map a value-set to a type-set without delta, and delta is precisely a class-hierarchy / allocation-site closed-world fact. So C2 does not supersede hierarchy analysis; it is parasitic on it, and inherits its closed-world requirement (any out-of-sight allocate, type-bound factory, or runtime-resolved generic-interface target that can reach the dispatch breaks delta and hence rho). A PC will see the claimed inversion collapse: the novel step still presupposes the very whole-program type analysis it is positioned against, and in a multi-hundred-TU separately-compiled build that closed-world delta is the hard, unargued part.
  - *Fix:* State explicitly in approach.tex/specialization.tex that delta is recovered under a closed-world assumption over the allocation/factory sites reachable to the dispatch, give the soundness condition (refuse when an allocation site is not enumerable, mirroring the glacial REFUSE rule), and reframe the CHA contrast as 'we add a value->type reduction on top of closed-world type flow' rather than 'we replace hierarchy analysis.' Desk-level honesty fix; the limitation itself is real but framing-correctable.

- **[answerable-now]** approach.tex line 30 asserts as a soundness result that 'residual guards partition the config space => exact, unguarded hot path', but variants.tex lines 7-8 reintroduce a per-call runtime guard whenever the cross-product cardinality exceeds threshold K (the high-cardinality variable falls back to a runtime guard), so the hot path is provably NOT unguarded in the very regime the cost model invokes to bound binary count.
  - *Why:* The 'exact, unguarded hot path' is stated as a theorem and is the linchpin of the 'pays no runtime cost' positioning and the unguarded-deployment novelty of C3. The K-threshold fallback contradicts it internally: above K, a guard runs every timestep on the hot path, which is the same per-call guard the paper repeatedly distinguishes itself from (RIFS/PGO). The partition claim and the blow-up-control mechanism are inconsistent, and a careful PC will read this as the soundness/positioning argument quietly conceding the guard it elsewhere disowns.
  - *Fix:* Reconcile the two sections: restate the partition theorem as covering only variables specialized into distinct binaries, and explicitly carve out the K-fallback variable as a deliberately guarded dimension, quantifying its per-timestep guard cost and arguing it is amortized/rare. Alternatively drop the unqualified 'unguarded hot path' phrasing and present unguardedness as a property of the per-cell binary modulo the bounded set of high-cardinality fallback guards.


## MINOR

- **[answerable-now]** Both experiments are single-translation-unit fixtures (the LU README itself says 'Single-TU NAS LU'; poly.f90 is one self-contained module) whose conclusions are generalized to 'multi-hundred-TU production Fortran', yet neither instantiates the multi-TU obstruction it names: the row-B linkage barrier ('another TU may write it') is never exercised against a real second TU that writes the global -- it is only the single-TU compiler's conservative assumption.
  - *Why:* The 'structural, not an implementation gap' argument generalizes a single-file IR-grep result to production-scale separate compilation without ever testing a genuine multi-TU build where the global is written in one unit and read in another, so the experiment cannot distinguish 'fundamentally unfoldable at scale' from 'single-TU flang is conservative' -- a much weaker, well-known claim.
  - *Fix:* Add a genuine multi-TU variant: split the config module, the init writer, and the compute readers into separately-compiled objects; run gfortran/flang/nvfortran with and without -flto and show whether real cross-TU separate compilation blocks folding, substantiating the 'multi-hundred-unit build' claim.

- **[framing-only]** C3's claimed novelty bundles three differentiators (greedy covering-set over a namelist distribution, a performance objective, and UNGUARDED multi-binary deployment), but 'unguarded' is not an independent algorithmic contribution of C3 -- it is a direct consequence of C1's glacial assumption that config never changes at runtime -- so once C1 is granted, the residual genuinely-new claim of C3 reduces to 'coverage maximization over a config distribution,' which the authors concede is SPL/covering-array prior art.
  - *Why:* The brief's own verdict puts C3's novelty on 'the combination,' explicitly hedging that set-cover-for-variants is SPL prior art. A prior-art skeptic decomposes the combination and finds the prongs are not independent: 'unguarded' falls out for free from C1 (no runtime check is needed precisely because the recovered values are glacial), and the 'namelist distribution' is just the config-domain instantiation of any input/feature distribution. That leaves only the coverage-maximization-with-a-performance-objective prong as the load-bearing novelty -- the very prong nearest Hochgraf-Pai (perf-weighted variant pre-selection) and SPL covering arrays. So C3's three-pronged differentiation collapses to one occupied-or-near-occupied prong, weakening the 'no prior system occupies this combination' claim.
  - *Fix:* Stop counting 'unguarded' as a separate C3 differentiator and present it as an inherited corollary of C1's glaciality; then defend C3 strictly on the perf-weighted coverage objective over an operational-namelist distribution and explicitly differentiate from Hochgraf-Pai and SPL covering arrays on that single axis (static unguarded binaries vs. their runtime-regressor-dispatched guarded variants / test-coverage objective). Writing/positioning fix; no new runs required.

- **[framing-only]** The 'deployment-time specialization as a third point in the compilation lifecycle (AOT/JIT/deployment-time)' framing is positioning rhetoric, not a technique: once values are recovered statically, the entire pipeline is ordinary ahead-of-time compilation, and the only thing that happens 'at deployment time' is name-based startup dispatch picking one pre-built binary -- a trivial mechanism with no new compilation work.
  - *Why:* The paper makes this lifecycle reframing a stated contribution ('We frame deployment-time specialization as a distinct point in the compilation lifecycle') and the conceptual home of the work. But AOT compilation has always consumed statically known inputs; reading a namelist at build time is config-file analysis feeding an otherwise standard AOT cascade. A sharp PC will note that nothing is specialized *at deployment* -- the binaries are built ahead of time and merely selected at startup (implementation.tex calls it 'name-based startup dispatch') -- so the 'third lifecycle point' collapses to 'AOT compilation with config-file-derived inputs,' undercutting the claim that this is a new paradigm rather than a useful static analysis plus a binary-selection trick.
  - *Fix:* Stop claiming a new lifecycle point as a contribution. Recast it as 'config-file-driven AOT specialization': the genuine content is (a) recovering values across the I/O barrier (C1) and (b) the covering set of unguarded binaries selected at startup (C3). Keep the AOT/JIT contrast as motivation for why static recovery beats runtime guards, but name the actual mechanism (build-time specialization + startup binary selection) rather than asserting a distinct compilation stage.

- **[framing-only]** The 'why this matters' GPU argument (intro) - knowing dimensions at compile time 'lets the GPU backend emit tighter kernels: constant launch bounds, unrolled loops, narrowed index types that a runtime-valued parameter forecloses' - credits these gains to deployment-time *config* recovery, but constant launch bounds, loop unrolling on known trip counts, and shape-specialized kernels are exactly what runtime-shape GPU JIT specializers already deliver (Proteus, CGO 2025, is cited in the brief as doing precisely kernel specialization on parameters, and PSyclone/DopingHPC JIT on runtime-live scalars), so the GPU benefit is attributable to *having a constant shape at codegen*, not to recovering it statically from a namelist.
  - *Why:* This conflates the value of the recovered constant with the value of the recovery mechanism - the same conflation the PC already flagged for C2's reachability claim, now recurring in the headline impact pitch. A skeptic will note that a runtime-shape JIT gets identical tighter kernels without any of C1/C2/C3, so the GPU-codegen advantage does not isolate the paper's contribution; the only genuine delta over a JIT is 'pays zero runtime compile cost', which is the lifecycle-positioning claim, not a codegen capability.
  - *Fix:* Reframe the GPU paragraph to claim only what is differential: the gains are the standard ones of a known-shape kernel, and the contribution is delivering that known shape *ahead of time, unguarded, from the config file* rather than via runtime JIT - explicitly distinguishing from Proteus/PSyclone. Desk-fixable in introduction.tex.

- **[answerable-now]** The LU table's row-C 'strongest baseline' (constant value present + whole-program LTO) is manufactured by hand-inserting source assignment statements (nz0=33, etc.) into dolu() via sed, i.e. it injects the value as an in-program store the compiler can see, so the table does not actually demonstrate that an LTO pipeline fed the recovered value through a realistic injection channel folds it — it demonstrates that a hand-edited source constant folds, which is a weaker and partly circular statement about the baseline the paper compares its scalar case against.
  - *Why:* The paper repeatedly designates 'recovered values injected as constants under whole-program LTO' as the strongest standard-tool baseline that isolates C1's scalar cascade; if that baseline is realized by editing source rather than by the actual injection mechanism the tool uses (and the row-A vs row-C contrast already concedes the cascade is standard), a PC can argue the LU experiment proves only that a literal in the source folds — which nobody disputes — and says nothing about whether the tool's injection path reproduces row C on a multi-TU config module, leaving the scalar-baseline comparison resting on a hand-constructed artifact.
  - *Fix:* Document that the row-C baseline and the tool's RQ4 LTO-injected-constant baseline use the same injection mechanism, and ideally construct row C by feeding the recovered constant through that mechanism rather than a sed source edit, so the 'strongest baseline' the paper measures its gain above is the real comparator and not a hand-edited literal.

- **[framing-only]** The conclusion's generality claim - 'beyond Fortran (C/C++ config structs, static-shaped Python/JAX configs)' - does not survive the paper's own framing, because the entire obstruction the contributions are built on (a `READ(NML=...)` I/O barrier depositing into externally-linked glacial *module* state) is specific to the Fortran namelist idiom: C/C++ codes parse config into ordinary heap/global structs with no namelist-read analog and where LTO/internalization behaves differently, and Python/JAX 'static-shaped configs' are already specialized by tracing/JIT (the very JIT the paper positions itself *against*), so the 'I/O barrier the compiler structurally cannot cross' premise has no counterpart there.
  - *Why:* The paper repeatedly leans on universality language ('production scientific codes', 'not DaCe-specific') to claim impact beyond a single language+frontend, but the structural-barrier argument that motivates C1 and the lifecycle framing is load-bearing *only* for Fortran namelists. A PC will read the C/C++/Python future-work line as evidence the authors have not checked whether the central obstruction even exists outside Fortran, weakening the claim that this is a general compilation technique rather than a Fortran-namelist point solution.
  - *Fix:* Either drop the cross-language generality gesture or, better, state honestly that the technique's precondition is a *recoverable, parse-then-freeze* config-ingestion idiom and analyze which of C/C++ struct-config / Python static-config actually exhibit the I/O-barrier-into-persisted-state pattern vs. being already JIT-specialized. Desk-fixable in conclusion.tex and a sentence in the scope discussion.

- **[answerable-now]** The motivating Listing 1 claim that the velocities are 'streamed from memory three times where once would do' overstates the fusion benefit: kernels A, B, and C write three different output arrays (vn_ie/z_kin_hor_e, z_vt_ie, z_w_concorr_me) and share only the reads of vn and vt, so fusing them saves re-reading the two shared inputs, not a 3x reduction of all memory traffic, and the loops span only nlev column points.
  - *Why:* The bandwidth-savings framing is the paper's opening hook and its qualitative justification for the whole approach on a 'memory-bandwidth-bound code,' but the distilled listing does not support a 3x traffic claim; a sharp PC will note the output-array writes are not shared and ask for the actual measured DRAM-traffic delta, which is not provided.
  - *Fix:* Rewrite the sentence to claim only what fusion delivers (eliminating redundant reloads of the shared vn/vt and the per-timestep config-branch tests), and defer any traffic-reduction factor to a measured number in RQ2's #2 fusion row rather than asserting 'three times where once would do' in the intro.

- **[framing-only]** The paper's title object and unifying-contribution name 'interval arithmetic on collections' misdescribes its own content: three of the four domain kinds (singleton, finite value-set, finite type-set) are not intervals but powerset/flat-lattice elements, and the 'collections' are the powerset abstraction itself, not collection-valued program data -- so the name reads as marketing for a generality the analysis does not have.
  - *Why:* Naming a contribution (and putting it in the title options) after a sub-case it mostly is not invites the reviewer to conclude the unification is rhetorical glue rather than technical content; combined with the authors' own approach.tex admission that the lattice is an instance of the reduced product, the grand name actively works against the paper by overclaiming a novel analysis where there is a standard product of standard domains plus one reduction rho.
  - *Fix:* Rename the unifying object to what it actually is -- e.g. a bounded disjunctive completion / reduced product of a value-interval domain with a finite type-set domain -- and reserve any 'interval' language for case #3; lead the contribution on the one genuinely new component (the value->type reduction rho) rather than on the umbrella name. This is purely a desk-time framing change; no eval needed.

